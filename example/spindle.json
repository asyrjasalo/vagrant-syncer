{
  "paths": [
    {
      "source": {
        "path": "files",
        "excludes": [
          "/excluded_file.txt",
          "/excluded_directory",
          "excluded_nested_file.txt",
          "excluded_nested_directory",
          "*.not"
        ],
        "initial": true,
        "continuous": true,
        "listener": {
          "verbose": false,
          "latency": 0.1
        }
      },
      "target": {
        "path": "files",
        "permissions": {
          "user": "rwX",
          "group": "rX",
          "other": ""
        },
        "args": {
          "rsync": ["--archive", "--delete", "--force"],
          "ssh": ["-o StrictHostKeyChecking=no", "-o UserKnownHostsFile=/dev/null"]
        }
      }
    }
  ]
}
